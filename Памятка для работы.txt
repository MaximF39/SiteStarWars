- Все странички находятся в templates.
- Reg и notreg - одни и те же страницы, котоыре отличаются только авторизованным пользователем.
- В static находятся все css, js, картинки, спрайты и php, если понадобится.
-- Navigpanel.css импортируется всегда! Является константой для нивигационной панели.
-- Все переменные (в основном цвета, но можно задать и иные, если понадобится) находятся в vars.css. Импортируется только в .css файлы.
-- Alls.css импортируется всегда! Является константой для всех страниц. 
-- Func.js имеет в себе все функции js.
- Все id и class должны размещатсья в самом начале.